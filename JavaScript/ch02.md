# （二）变量
```js
        计算机代码运行在内存中        
        javascript变量中并不存储任何值，存储值的内存地址！变量：值（值是地址0x11）
        和Java不同
    
        //存的同一个内存地址，先去内存找有没有"哈哈"，有的话就存地址，没有就开辟新的,目的是不浪费内存

        let a = '哈哈'
        let b = '哈哈'
```
```js
qq = 12345; //这样声明的是全局变量

const PI = 3.1415926 //在JS中，使用const声明常量，常量只能赋值一次，重复赋值会报错

```

